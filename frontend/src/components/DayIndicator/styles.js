const SIZE = 10;

export default {
  circle: {
    width: SIZE,
    minWidth: SIZE,
    height: SIZE,
    minHeight: SIZE,
    backgroundColor: '#89CFF0',
    borderRadius: '50%',
    marginRight: 10,
    boxSizing: 'border-box',
  },
};
