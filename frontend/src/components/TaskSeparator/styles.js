const SIZE = 6;

export default {
  circle: {
    width: SIZE,
    minWidth: SIZE,
    height: SIZE,
    minHeight: SIZE,
    backgroundColor: 'grey',
    border: (theme) => `2px solid #D3D3D3`,
    borderRadius: '50%',
    marginRight: 10,
    boxSizing: 'border-box',
  },
};
